---
description: Cross-asset views with a falsifying signpost each (no weights, no ratings)
argument-hint: "[资产范围与期限, e.g. 股债 未来6个月]"
---

Load the `asset-allocation` skill and assemble cross-asset views for: $ARGUMENTS

If the horizon is unstated, ask once before building. Asset classes with no retrievable series are reported 源不可用 rather than given a view.
