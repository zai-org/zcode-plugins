---
description: Build a sources and uses schedule with the pro-forma capital structure for a transaction
argument-hint: "[收购方] [标的] [交易规模或融资安排]"
---

Load the `sources-uses` skill and build the sources and uses schedule for: $ARGUMENTS

If it is unclear whether existing target debt is refinanced, assumed, or left in place, ask before building — that election changes the uses side more than anything else.
