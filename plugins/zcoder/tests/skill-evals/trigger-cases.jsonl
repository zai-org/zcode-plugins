{"id": "t01-laravel-migration", "utterance": "I need a Laravel migration that adds a decimal money column with a foreign key to the users table.", "expected": "laravel-dev", "note": "migration + money column + FK is laravel-dev's core trigger domain"}
{"id": "t02-laravel-scaffold", "utterance": "generate a model with migration, factory and policy for a Product entity in my Laravel app", "expected": "laravel-dev", "note": "artisan scaffolding phrasing (make:model -a equivalent)"}
{"id": "t03-yaml-convert", "utterance": "convert this config.yaml to JSON for the terraform pipeline, keep the values exactly as they are", "expected": "yaml-json-convert", "note": "explicit YAML→JSON conversion request; 'keep values exactly' is the type-strictness signal"}
{"id": "t04-yaml-types", "utterance": "after converting my YAML to JSON the version number 2.0 turned into 2 — how do I stop that?", "expected": "yaml-json-convert", "note": "quoted-scalar type drift complaint — the skill's named failure mode"}
{"id": "t05-route-screenshot", "utterance": "inspect this screenshot of the broken login page, implement the CSS fix, and then verify it against the mockup", "expected": "glm-orchestrator", "note": "REV2 (after router-r1-2026-09-04): the v1 utterance was a single-image inspection and the router correctly chose none — the glm-orchestrator description claims only multi-part (2+) work, so the original expectation was mis-registered. This revision uses a genuinely multi-part request (inspect + implement + verify), which the description does claim."}
{"id": "t6-turbo-rejection", "utterance": "Why did glm-turbo reject my dispatches?", "expected": "glm-orchestrator", "note": "carried over from the 2026-09-03 dry run where the router chose glm-orchestrator and the adjudication confirmed it — the engine rejection matrix lives in that skill's body; knowledge routing, not task routing"}
{"id": "t07-forge-scan", "utterance": "scan our Django project and tell me which coding-convention skills we're missing", "expected": "skill-forge", "note": "stack-gap scan is /skill-scan's exact job"}
{"id": "t08-forge-evolve", "utterance": "run an improvement round on the yaml-json-convert skill and show me the promotion gates", "expected": "skill-forge", "note": "evolution round + gates vocabulary = skill-forge"}
{"id": "t09-none-smalltalk", "utterance": "what's a good name for a rescue cat?", "expected": "none", "note": "pure conversation — no skill should load (unwanted-load tripwire)"}
{"id": "t10-none-generic", "utterance": "summarize this paragraph in one sentence for the standup notes", "expected": "none", "note": "generic writing task inside every agent's base ability — a near-miss bait for orchestrator/skill keywords"}
{"id": "t11-yaml-bait", "utterance": "read this JSON file and change the value of the timeout key to 30", "expected": "none", "note": "JSON editing is base ability; bait: 'JSON file' keyword must not trigger the YAML conversion skill"}
{"id": "t12-laravel-bait", "utterance": "my PHP script prints a broken array — help me debug the foreach loop", "expected": "none", "note": "plain PHP debugging is not Laravel convention work; bait: 'PHP' keyword must not trigger laravel-dev"}
